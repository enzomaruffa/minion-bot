# Project Setup

Set up the foundational project structure with pyproject.toml, uv configuration, folder structure, environment variables, and gitignore.

## Acceptance Criteria
- [ ] pyproject.toml with all dependencies (agno, python-telegram-bot, sqlalchemy, apscheduler, openai, google-api-python-client)
- [ ] Folder structure: src/, data/, credentials/
- [ ] .env.example with all required environment variables
- [ ] .gitignore ignoring .env, data/, credentials/, __pycache__
- [ ] Empty __init__.py files in all packages
